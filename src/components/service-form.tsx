"use client";
import { useActionState, useState } from "react";
import { useFormStatus } from "react-dom";
import { BriefcaseBusiness, LoaderCircle } from "lucide-react";
import { saveServiceAction, type ServiceActionState } from "@/app/app/service-actions";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Textarea } from "@/components/ui/textarea";
const initialState: ServiceActionState = { status: "idle" };
function Submit({ editing }: { editing: boolean }) { const { pending } = useFormStatus(); return <Button type="submit" className="w-full bg-violet-600 hover:bg-violet-700" disabled={pending}>{pending ? <LoaderCircle className="animate-spin" /> : <BriefcaseBusiness />}{editing ? "Guardar cambios" : "Crear servicio"}</Button>; }
export function ServiceForm({ companies, categories, fixedCompanyId, service }: { companies: { id: string; name: string }[]; categories: { id: string; companyId: string; name: string }[]; fixedCompanyId?: string; service?: { id: string; companyId: string; categoryId: string | null; code: string; name: string; description: string | null; durationMinutes: number; priceCents: number; status: "active" | "inactive"; isPublic: boolean } }) { const [state, action] = useActionState(saveServiceAction, initialState); const [companyId, setCompanyId] = useState(service?.companyId ?? fixedCompanyId ?? ""); const availableCategories = categories.filter((category) => category.companyId === companyId); return <Card><CardHeader><CardTitle>{service ? "Editar servicio" : "Nuevo servicio"}</CardTitle><CardDescription>Precio, duración, visibilidad y categoría del servicio.</CardDescription></CardHeader><CardContent><form action={action} className="space-y-4"><input type="hidden" name="serviceId" value={service?.id ?? ""} />{fixedCompanyId ? <input type="hidden" name="companyId" value={fixedCompanyId} /> : <div className="space-y-2"><Label htmlFor="serviceCompany">Empresa</Label><select id="serviceCompany" name="companyId" value={companyId} onChange={(event) => setCompanyId(event.target.value)} required className="h-9 w-full rounded-md border border-input bg-transparent px-3 text-sm"><option value="" disabled>Seleccionar</option>{companies.map((company) => <option key={company.id} value={company.id}>{company.name}</option>)}</select></div>}<div className="grid gap-4 sm:grid-cols-2"><div className="space-y-2"><Label htmlFor="serviceCode">Código</Label><Input id="serviceCode" name="code" defaultValue={service?.code} required maxLength={48} /></div><div className="space-y-2"><Label htmlFor="serviceCategory">Categoría</Label><select id="serviceCategory" name="categoryId" defaultValue={service?.categoryId ?? ""} className="h-9 w-full rounded-md border border-input bg-transparent px-3 text-sm"><option value="">Sin categoría</option>{availableCategories.map((category) => <option key={category.id} value={category.id}>{category.name}</option>)}</select></div></div><div className="space-y-2"><Label htmlFor="serviceName">Nombre</Label><Input id="serviceName" name="name" defaultValue={service?.name} required maxLength={160} /></div><div className="space-y-2"><Label htmlFor="serviceDescription">Descripción</Label><Textarea id="serviceDescription" name="description" defaultValue={service?.description ?? ""} rows={3} maxLength={2000} /></div><div className="grid gap-4 sm:grid-cols-2"><div className="space-y-2"><Label htmlFor="serviceDuration">Duración (min)</Label><Input id="serviceDuration" name="durationMinutes" type="number" min={5} max={720} step={5} defaultValue={service?.durationMinutes ?? 60} required /></div><div className="space-y-2"><Label htmlFor="servicePrice">Precio</Label><Input id="servicePrice" name="price" type="number" min={0} step="0.01" defaultValue={service ? (service.priceCents / 100).toFixed(2) : "0.00"} required /></div></div><div className="grid gap-4 sm:grid-cols-2"><div className="space-y-2"><Label htmlFor="serviceStatus">Estado</Label><select id="serviceStatus" name="status" defaultValue={service?.status ?? "active"} className="h-9 w-full rounded-md border border-input bg-transparent px-3 text-sm"><option value="active">Activo</option><option value="inactive">Inactivo</option></select></div><label className="mt-7 flex items-center gap-2 rounded-lg border p-3 text-sm"><input type="checkbox" name="isPublic" defaultChecked={service?.isPublic ?? true} className="size-4 accent-violet-600" />Disponible para reserva pública</label></div>{state.status !== "idle" ? <p role="status" className={state.status === "success" ? "rounded-lg border border-emerald-200 bg-emerald-50 px-3 py-2 text-sm text-emerald-800" : "rounded-lg border border-red-200 bg-red-50 px-3 py-2 text-sm text-red-700"}>{state.message}</p> : null}<Submit editing={Boolean(service)} /></form></CardContent></Card>; }
